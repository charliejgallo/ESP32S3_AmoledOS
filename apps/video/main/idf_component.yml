dependencies:
  idf: ">=5.5,<6.1"
  espressif/elf_loader: "^1.3.3"
  lvgl/lvgl: "==9.5.0"
  # Only for its header: the decoder itself lives in the firmware and the
  # .so reaches it through the symbol table (tools/gen_symbols.py).
  espressif/esp_new_jpeg: "^1.0.2"
